    <ProjectConfiguration Include="#{REPLACEME:configuration}|#{REPLACEME:bitness}">
      <Configuration>#{REPLACEME:configuration}</Configuration>
      <Platform>#{REPLACEME:bitness}</Platform>
    </ProjectConfiguration>
